/** Master switch — set false to disable all scan limits and pricing UI. */
export const MONETIZATION_PAUSED = false;

/** 1 = Daily Free Scan + Pay as you go only. Later phases add subscriptions. */
export const MONETIZATION_PHASE = 1;
